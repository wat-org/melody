# remove files generated by the melody project build
# in the case of the archives directory, we exclude all archived files because they are tracked by GIT
*

# Do not ignore .gitignore file
!.gitignore
